<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.xgouchet.khronorg">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activities.ProjectDetailsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activities.ProjectEditorActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activities.TravellerEditorActivity"
            android:exported="false" />

        <!-- Content Provider  -->
        <provider
            android:name=".provider.KhronorgContentProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false" />
    </application>

</manifest>
